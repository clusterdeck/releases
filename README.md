# Clusterdeck releases

This repo hosts **built binaries only** for [Clusterdeck](https://github.com/clusterdeck/app) —
a multi-cluster SSH workspace app. No source code lives here.

Install via Homebrew:

```sh
brew install --cask clusterdeck/tap/clusterdeck
```

See [Releases](../../releases) for direct `.dmg` downloads.
